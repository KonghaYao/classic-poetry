import { Form } from "@arco-design/web-react";
import { FC } from "react";

export const SourceManager: FC<{}> = () => {
    return (
        <Form>
            <Form.Item label="全局字体种类"></Form.Item>
        </Form>
    );
};
